{{#each tabs as |tab|}}
  {{log tab}}
  <!-- {{> partial}} -->

  <tr data-id="{{tab.id}}"
    data-date="{{tab.date}}"
    data-project_name="{{tab.project_name}}"
    data-task="{{tab.task}}"
    data-time_spent="{{tab.time_spent}}"
    data-notes="{{tab.notes}}">
    <td class="info-td" data-toggle="modal" data-target="#modifyTabModal">
      {{#if tab.date}}
        {{tab.date}}
      {{else}}
        —
      {{/if}}
    </td>
    <td class="info-td" data-toggle="modal" data-target="#modifyTabModal">
      {{#if tab.project_name}}
        {{tab.project_name}}
      {{else}}
        —
      {{/if}}
    </td>
    <td class="info-td" data-toggle="modal" data-target="#modifyTabModal">
      {{#if tab.task}}
        {{tab.task}}
      {{else}}
        —
      {{/if}}
    </td>
    <td class="info-td" data-toggle="modal" data-target="#modifyTabModal">
      {{#if tab.time_spent}}
        {{tab.time_spent}}
      {{else}}
        —
      {{/if}}
    </td>
    <td class="info-td" data-toggle="modal" data-target="#modifyTabModal">
      {{#if tab.notes}}
        {{tab.notes}}
      {{else}}
        —
      {{/if}}
    </td>
    <td class="info-button"><button type="button" class="modify-tab-button" id="modify-tab-button" data-toggle="modal" data-target="#modifyTabModal">Modify</button></td>
  </tr>

{{/each}}
