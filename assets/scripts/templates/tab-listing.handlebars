{{#each tabs as |tab|}}
  {{log tab}}
  <!-- {{> partial}} -->
  <!-- Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{tab.id}}"> <small>Project: {{tab.project_name}}</small></h1>

  <ul data-id="{{tab.id}}">
    <li>{{#if tab.task}}
      {{tab.task}}
    {{else}}
      No author provided, bruh
    {{/if}}</li>
    <li><button type="submit">Remove</button></li>
  </ul>

  <!-- This was the former code for just get and clear… -->
  <!-- <div>
    {{#if tab.author}}
      {{tab.author}}
    {{else}}
      No author provided.
    {{/if}}
  </div> -->

{{/each}}
